"use client"; import { List, useTable, EditButton, ShowButton, DeleteButton } from "@refinedev/antd"; import { Table, Space, Image, Input } from "antd"; import { Authenticated } from "@refinedev/core"; import { useRouter } from "next/navigation"; import { SearchOutlined } from "@ant-design/icons"; export default function ProductsList() { const router = useRouter(); const { tableProps, searchParams } = useTable({ resource: "products", filters: { initial: [{ field: "name", value: "", operator: "contains" }], }, }); return (<Authenticated key="products-list" fallback={<div className="flex items-center justify-center min-h-screen">لطفاً وارد شوید...</div>}><List title="مدیریت محصولات"><Input placeholder="جستجوی محصول..." prefix={<SearchOutlined />} style={{ marginBottom: 16, maxWidth: 300 }} value={searchParams?.filters?.find((f) => f.field === "name")?.value || ""} onChange={(e) => { const newFilters = searchParams?.filters?.filter((f) => f.field !== "name") || []; if (e.target.value) { newFilters.push({ field: "name", value: e.target.value, operator: "contains" }); } tableProps.pagination?.current && tableProps.pagination.current !== 1 ? tableProps.onChange?.({ current: 1, pageSize: tableProps.pagination.pageSize }) : tableProps.onChange?.({}); }} /><Table {...tableProps} rowKey="id" scroll={{ x: 800 }}><Table.Column dataIndex="id" title="ID" width={60} /><Table.Column dataIndex="image" title="تصویر" width={100} render={(value: string) => <Image width={60} height={60} src={value} alt="product" style={{ objectFit: "cover" }} />} /><Table.Column dataIndex="name" title="نام محصول" /><Table.Column dataIndex="price_toman" title="قیمت (تومان)" render={(value: number) => value?.toLocaleString("fa-IR")} /><Table.Column dataIndex="brand" title="برند" /><Table.Column dataIndex="stock" title="موجودی" /><Table.Column title="عملیات" fixed="right" render={(_, record: any) => (<Space><EditButton size="small" recordItemId={record.id} /><ShowButton size="small" recordItemId={record.id} /><DeleteButton size="small" recordItemId={record.id} /></Space>)} /></Table></List></Authenticated>); }
