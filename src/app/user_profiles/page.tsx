"use client"; import { List, useTable, ShowButton } from "@refinedev/antd"; import { Table, Space, Tag } from "antd"; import { Authenticated } from "@refinedev/core"; import { useRouter } from "next/navigation"; export default function UserProfilesList() { const router = useRouter(); const { tableProps } = useTable({ resource: "user_profiles" }); return (<Authenticated key="user-profiles-list" fallback={<div className="flex items-center justify-center min-h-screen">لطفاً وارد شوید...</div>} onRedirect={() => router.push("/login")}><List title="مدیریت کاربران"><Table {...tableProps} rowKey="id"><Table.Column dataIndex="id" title="ID" width={100} /><Table.Column dataIndex="user_id" title="User ID" width={200} /><Table.Column dataIndex="role" title="نقش" render={(value: string) => <Tag color={value === "admin" ? "red" : "blue"}>{value}</Tag>} /><Table.Column dataIndex="phone" title="تلفن" /><Table.Column dataIndex="address" title="آدرس" /><Table.Column dataIndex="created_at" title="تاریخ ثبت‌نام" render={(value: string) => new Date(value).toLocaleDateString("fa-IR")} /><Table.Column title="عملیات" render={(_, record: any) => (<Space><ShowButton size="small" recordItemId={record.id} /></Space>)} /></Table></List></Authenticated>); }
